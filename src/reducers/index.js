import { START_CHALLENGE, MAKE_GUESS } from "../actions";



function reducer(state = {}, action) {
    switch (START_CHALLENGE) {
        case value:
            
            break;
        case (MAKE_GUESS):

            break;
        default:
            break;
    }
}